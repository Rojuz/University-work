#include <cmath>
#include <iomanip>
#include <iostream>
using namespace std;

int main()
{
    double  D;
    double a, b, c,x1,x2;
    cin >> a >> b >> c;
   
    D = (b * b) - 4 * a * c;
    
    if(D<0)
    {
        cout << "NO";
     
    }
   
    else if (D==0)
   {
       x1 = (-b)/(2*a);
cout << x1;
   }
   else {
       x1 = ( - b + sqrt(D)) / (2 * a);
       x2 = ( - b - sqrt(D)) / (2 * a);
      
   }
    if(x1>x2)
    {
        cout << x2 << "_" << x1;
    }
    else
    {
        cout << x1 << "_" << x2;
    }
}
